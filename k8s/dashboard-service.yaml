apiVersion: v1
kind: Service
metadata:
  name: review-analyzer-dashboard
  namespace: review-analyzer
spec:
  selector:
    app: review-analyzer-dashboard
  ports:
    - name: http
      port: 8501
      targetPort: 8501
  type: ClusterIP
